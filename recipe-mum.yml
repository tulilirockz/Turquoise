name: turquoise-mum
base-image: ghcr.io/ublue-os/deepin-main
fedora-version: 38
description: A turquoise image for my mom!

scripts:
  pre:
    - autorun.sh
  post:
    - autorun.sh
  
rpm:
  install:
    - waydroid
    - weston
    - tmux
    - cockpit
    - cockpit-podman
    - tailscale
 
  remove:
    -

firstboot:
  yafti: false
  flatpaks: